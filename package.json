{
  "name": "url-analyzer-cli",
  "version": "1.0.0",
  "description": "A CLI tool to analyze URL redirect chains and generate an HTML report.",
  "main": "dist/index.js",
  "scripts": {
    "build": "tsc --project tsconfig.json",
    "start": "node dist/index.js",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "playwright-chromium": "^1.44.1",
    "validator": "^13.12.0",
    "ip-range-check": "^0.2.0"
  },
  "devDependencies": {
    "@types/validator": "^13.11.10",
    "typescript": "^5.4.5"
  }
}
