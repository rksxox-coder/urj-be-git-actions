name: Redirect Analysis Tool

on:
  workflow_dispatch:
    inputs:
      url_list:
        description: 'Paste URLs here (one per line)'
        required: true
        default: 'https://www.bmw.com'

permissions:
  contents: write

jobs:
  analyze:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '18'

      - name: Install Dependencies
        run: npm install

      - name: Install Playwright Browsers
        run: npx playwright install chromium

      - name: Create URL Input File
        # This takes the input from the API and saves it to a file
        run: |
          echo "${{ github.event.inputs.url_list }}" > urls.txt

      - name: Run Analysis Script
        run: npx ts-node src/main.ts

      - name: Save Reports to Repo
        run: |
          git config --global user.name "GitHub Actions Bot"
          git config --global user.email "actions@github.com"
          
          # Pull latest changes first to avoid conflicts (Rebase strategy)
          git pull --rebase
          
          # Add the new nested report and the history file
          git add reports/
          git add history.json
          
          git commit -m "Add Scan Report [skip ci]" || echo "No changes"
          git push
